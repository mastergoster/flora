{% extends 'layout/admin.twig' %}

{% block options %}
	<h1 class="h2">modifheure</h1>
{% endblock %}

{% block content %}

	<div class="table-responsive col-4">
		<table class="table table-striped table-sm text-center">
			<thead>
				<tr>
					<th>date</th>

				</tr>
			</thead>
			<tbody>
				<tr>
					<td class="form-inline">
						<form class="form-inline" method="post">
							<div class="form-group mx-sm-3 mb-2">
								<input name="date" type="text" class="form-control" id="date1" placeholder="Y-m-d H:i:s">
							</div>
							<button type="submit" class="btn btn-primary mb-2">
								<span data-feather="check-square"></span>
							</button>
						</form>
					</td>
				</tr>

				{% for key, lines in items %}
				</tbody>
			</tbody>
			<thead>
				<tr>
					<th>{{ key | date("d/m/Y")}}</th>

				</tr>
			</thead>
			{% for item in lines %}
				<tr>
					<td class="form-inline">
						<form class="form-inline" method="post">
							<div class="form-group mb-2">
								<input name="id" type="hidden" class="form-control" id="id" value="{{ item.id }}">
							</div>
							<div class="form-group mx-sm-3 mb-2">
								<input name="date" type="text" class="form-control" id="date" value="{{ item.createdAt | date("Y-m-d H:i:s") }}" placeholder="Password">
							</div>
							<button type="submit" class="btn btn-primary mb-2">
								<span data-feather="check-square"></span>
							</button>
						</form>
						<form class="form-inline" method="post">
							<div class="form-group mb-2">
								<input name="id" type="hidden" class="form-control" id="id" value="{{ item.id }}">
							</div>
							<button type="submit" class="btn btn-danger mb-2">
								<span data-feather="trash-2"></span>
							</button>
						</form>
					</td>
				</tr>

			{% endfor %}
		{% endfor %}
	</tbody>
</table></div>{% endblock %}
